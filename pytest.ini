[pytest]
testpaths = tests
addopts = -m "not integration" --cov=app --cov=bot --cov-report=term
markers =
    integration: اختبارات تتصل فعليًا بـ Gemini/الشبكة — تُشغَّل يدويًا فقط (pytest -m integration)