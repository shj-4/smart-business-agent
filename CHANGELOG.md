# سجل التغييرات

التغييرات تُجمَّع حسب الدورات. لم نصل إلى إصدار 1.0 بعد.

## الجولة 13 — الديون والميزانيات والحدود والتنبؤات والصحة

- **الديون (#21)**: `/debts` ملخّص مدين/دائن لكل شخص مع المقاصة والفرق
  (income − expense)، وتنبيهات آلية عند تغيّر الوضع.
- **ميزانيات بالنطاق (#23)**: ميزانية تصنيف لشهر أو فترة (12‏شهرًا/كل الوقت)
  وتنبيهات تجاوز شهرية.
- **الحدود الائتمانية (#26)**: `/credit` والتحقق التلقائي عند اقتراب تجاوز
  حد العميل مع تنبيه.
- **الفواتير الآجلة (#22)**: بداية/استحقاق، ترحيل تلقائي للمتأخرة، وسداد
  عبر `/invoices`.
- **دورة الطلبيات (#38)**: حالة الطلبية كاملة (open → paid/done) عبر `/orders`
  مع تسجيل السداد.
- **ضريبة القيمة المضافة (#24)**: استخراج نسبة/قيمة VAT عند التسجيل وحفظها.
- **سعر صرف تاريخي (#25)**: تثبيت سعر الصرف لحظة التسجيل للتقارير الموحّدة.
- **الصحة والاستخدام (#27/32)**: `/health` فحص النظام عند الطلب وعند الإقلاع،
  و`/stats` إحصائيات الاستخدام حسب مساحة العمل.
- **إعادة تطبيق الهجرات (#28)**: `python scripts/reapply_migrations.py`
  (يُعيد بناء قاعدة البيانات من الهجرات خطوة بخطوة مع سطر أوامر خيارات).
- **التنبؤات (#36)**: `/forecast` انحدار خطي إرشادي لمصروفات/إيرادات الأشهر
  القادمة لكل عملة (+ التوحيد بعملة الأساس إن أمكن).
- **انحراف الإنفاق (#37)**: `/deviation` مقارنة الشهر الحالي بمتوسط آخر
  3 أشهر، وتنبيه يومي تلقائي عند تجاوز حدّ (30٪).
- **تصدير PDF (#35)**: `/export pdf [period]` عبر reportlab مع خط عربي
  وتشكيل/اتجاه (arabic-reshaper + python-bidi).
- إصلاح: إعادة تمكين مسجّل التدقيق بعد تشغيل fileConfig الخاص بـ alembic.

## الجولة 12 — بنية التشغيل والخارطة الشاملة

- فصل البيئات: `APP_ENV` ينتج SQLite منفصلًا (business.db للتطوير،
  business_staging.db / business_production.db للبيئات الأخرى).
- نسخ احتياطي يومي تلقائي (7 نسخ) عبر `app/database/backup.py` +
  مهمة مجدولة؛ و`scripts/backup_db.py` سطر أمر يدوي.
- اختبارات هجرات: كل ملف هجرة يحتوي `downgrade` وسلسلة متصلة.
- أمر `/help` مع دليل استخدام، وتلميح في `/start`.
- تجربة «آخر العمليات»: صفحة تصفّح (الأحدث ← الأقدم) مع سطر تعديل/حذف لكل
  سجل، وحذف ناعم بسجل تدقيق.
- بحث نصي في السجلات (نتائج صفحات + تعديل/حذف) مع حارس نشاط.
- لغة واجهة ar/en أساسية (قوائم رئيسية/أدوات) مخزنة في `user_prefs`
  ومستعادة عبر كاش؛ أمر `/lang`.
- حد رسائل كلي للعملية فوق حدّ المستخدم (حماية إغراق).
- حارس حقن برومبت: أنماط شائعة تُحوَّل الرسالة إلى محادثة عامة + بند أمان
  مضاف إلى برومبت النظام.
- سجلات JSON اختيارية (`LOG_FORMAT=json`).
- فصل نصوص الـ AI إلى `prompts/*.md` مع محمّل يسقط آمنًا إلى القيم المدمجة.
- اختبار E2E خفيف (رسالة → تأكيد → حفظ → آخر العمليات → حذف) + تغطية
  بـ pytest‑cov، وتوثيق README/CHANGELOG/.env.example.

## قبل ذلك

- تعديل «آخر سجل» بأزرار حقول (money/currency/person/category/description/
  …) + شارات أولوية وتكرار في سطور المهام.
- إكمال مساحة العمل المشتركة، التقارير الدورية، تصدير Excel، الرسم البياني،
  تعديل التأكيد، طابور AI، تصحيح الملاحظات، Sentry، وسلاسل اللهجات.
- طباعة ملاحظة: لا يوجد إصدار 1.0 حتى الآن؛ طريقة العمل دورة-تجريبية.