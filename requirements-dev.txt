# Development-only dependencies (linting, testing, coverage). Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
pytest-mock==3.15.1
pytest-cov==7.1.0
ruff==0.16.6